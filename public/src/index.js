import React from 'react';
import ReactDOM from 'react-dom';
import SIDIntel from './App';

ReactDOM.render(
    <React.StrictMode>
        <SIDIntel />
    </React.StrictMode>,
    document.getElementById('root')
);
